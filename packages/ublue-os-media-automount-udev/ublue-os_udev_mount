#!/bin/bash

# This script ensures we are not mounting ntfs partitions

dev=$1
/usr/bin/udevadm settle
fstype=$(/usr/bin/lsblk -n -o FSTYPE "$dev")
if [[ $fstype != ntfs ]]; then
    /usr/bin/systemd-mount --collect --discover "$dev"
fi
